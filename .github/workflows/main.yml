name: Yocto Check

on:
  push:
    branches:
      - main  # Le nom de votre branche principale

jobs:
  build-and-test:
    runs-on: ubuntu-20.04

    steps:
    - name: Checkout du code
      uses: actions/checkout@v3
      
    - name: Set up Python
      uses: actions/setup-python@v3  # Utilisez la version 2 de l'action setup-python
      with:
        python-version: 3.x  # Spécifiez la version Python que vous utilisez
      

    - name: Installing my environnment 1 
      run: |
        chmod +x install_env1.sh
        ./install_env1.sh

    - name: Installing my environnment 2 
      run: |
        chmod +x install_env2.sh
        ./install_env2.sh


    - name: Installing my environnment 3 
      run: |
        chmod +x install_env3.sh
        ./install_env3.sh

    - name: Archives after save
      run: python test_probe.py




        